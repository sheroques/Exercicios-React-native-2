import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ecf0f1', // Cor de fundo leve
    paddingTop: 10,
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#e74c3c', // Cor vermelha para o título principal
    textAlign: 'center',
    marginVertical: 20,
  },
});

export default styles;